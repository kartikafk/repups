export async function predictAnomaly({ exerciseId, plane, features }) { const base=process.env.ML_SERVICE_URL; if(!base)return {isAnomaly:null,reason:"ml_service_not_configured"}; try { const response=await fetch(`${base.replace(/\/$/,"")}/ml/v1/predict/anomaly`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({exerciseId,plane,features})}); return response.ok?await response.json():{isAnomaly:null,reason:"ml_service_unavailable"}; } catch { return {isAnomaly:null,reason:"ml_service_unavailable"}; } }
