| Committed | Not committed |
|---|---|
| ML Python source, Dockerfiles, Compose, Node/React integration | `ml-service/app/datasets/` and `ml-service/app/models_store/` |

Datasets contain user posture data and models are generated artifacts. Regenerate them locally or per deployment. A future TODO is object storage such as S3/GCS for sharing trained models; it is not implemented.
