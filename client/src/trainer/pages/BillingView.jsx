import {useEffect,useState}from"react";import{apiUrl}from"../../config";import{authHeaders}from"../../api";import{C}from"../theme";import{Card}from"../components";
export default function BillingView(){const[data,setData]=useState(null);useEffect(()=>fetch(apiUrl("billing"),{headers:authHeaders()}).then(r=>r.json()).then(setData).catch(()=>setData({totalPaid:0,outstanding:0,nextPayout:0,transactions:[]})),[]);if(!data)return <p style={{color:C.sub}}>Loading billing…</p>;return <div><h2 style={{color:C.text}}>Billing</h2><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(150px,1fr))",gap:12}}>{[["Next Payout",data.nextPayout],["Total Paid",data.totalPaid],["Outstanding",data.outstanding]].map(([label,value])=><Card key={label} style={{padding:18}}><b style={{fontSize:22,color:C.lime}}>₹{Number(value||0).toLocaleString()}</b><div style={{color:C.sub,fontSize:12}}>{label}</div></Card>)}</div><Card style={{padding:20,marginTop:12,color:C.sub}}>{data.transactions?.length?data.transactions.map(t=><div key={t._id}>{t.description}</div>):"No payments yet"}</Card></div>}
